{
  "name": "srmall-capstone",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "npm run dev -w web",
    "lint": "npm run lint --workspaces --if-present",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "db:generate": "npm run db:generate -w @srmall/database",
    "db:push": "npm run db:push -w @srmall/database",
    "db:seed": "npm run db:seed -w @srmall/database",
    "db:reset": "npm run db:reset -w @srmall/database",
    "setup": "docker-compose up -d db && npm run db:generate && npm run db:push && npm run db:seed",
    "postinstall": "cd packages/database && npx prisma generate"
  },
  "devDependencies": {
    "prettier": "latest",
    "turbo": "^2.9.14"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.103.3",
    "@types/node": "20.19.37",
    "@types/nodemailer": "^8.0.0",
    "enhanced-resolve": "^5.20.1",
    "jspdf": "^4.2.1",
    "jspdf-autotable": "^5.0.7",
    "nodemailer": "^8.0.5",
    "swr": "^2.4.1"
  },
  "optionalDependencies": {
    "@next/swc-linux-x64-gnu": "16.2.0",
    "@next/swc-win32-x64-msvc": "16.2.0",
    "sharp": "^0.34.5"
  }
}
